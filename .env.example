# Trading Bot Configuration
PAPER_TRADING=true
GAP_THRESHOLD=3.0
MAX_POSITION_SIZE=1000
MAX_POSITIONS=5

# Database
DB_PATH=/app/data/skim.db

# ============================================================
# OAuth 1.0a Authentication (Recommended)
# ============================================================
# OAuth connects directly to IBKR API - no Gateway needed!
# Generate OAuth credentials at: https://www.interactivebrokers.com/portal
# Follow the guide: https://github.com/Voyz/ibind/wiki/OAuth-1.0a

# Consumer key from IBKR (e.g., PSKIMMILK)
OAUTH_CONSUMER_KEY=your_consumer_key

# Access token and secret from IBKR
OAUTH_ACCESS_TOKEN=your_access_token
OAUTH_ACCESS_TOKEN_SECRET=your_access_token_secret

# Paths to your .pem key files (generate with openssl)
OAUTH_SIGNATURE_PATH=/opt/skim/oauth_keys/private_signature.pem
OAUTH_ENCRYPTION_PATH=/opt/skim/oauth_keys/private_encryption.pem

# DH prime extracted from dhparam.pem (hex string without spaces/colons)
OAUTH_DH_PRIME=your_dh_prime_hex_string

# ============================================================
# Session-Based Authentication (Legacy - requires Gateway)
# ============================================================
# Only needed if NOT using OAuth above

# IB Gateway credentials
IB_USERNAME=your_ib_username
IB_PASSWORD=your_ib_password

# IBeam / Client Portal API Connection
IB_HOST=ibeam
IB_PORT=5000
IB_CLIENT_ID=1
