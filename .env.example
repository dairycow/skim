# ============================================================
# OAuth 1.0a Authentication for IBKR Client Portal API
# ============================================================
# Direct OAuth connection to IBKR API - no Gateway needed!
# Generate OAuth credentials at: https://www.interactivebrokers.com/portal

# Consumer key from IBKR (e.g., PSKIMMILK)
OAUTH_CONSUMER_KEY=your_consumer_key

# Access token and secret from IBKR OAuth setup
OAUTH_ACCESS_TOKEN=your_access_token
OAUTH_ACCESS_TOKEN_SECRET=your_encrypted_access_token_secret

# RSA key files are expected at: /opt/skim/oauth_keys/
# Generate with: openssl genrsa -out oauth_keys/private_signature.pem 2048
#                openssl genrsa -out oauth_keys/private_encryption.pem 2048
# Keys are automatically mounted in Docker container from local oauth_keys/ directory

# Diffie-Hellman prime (hex string from IBKR, no spaces/colons)
OAUTH_DH_PRIME=your_dh_prime_hex_string

# Discord webhook URL for notifications (optional)
# Create webhook at: https://support.discord.com/hc/en-us/articles/228383668-Intro-to-Webhooks
DISCORD_WEBHOOK_URL=your_discord_webhook_url
